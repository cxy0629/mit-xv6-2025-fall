1. 13存放在a2寄存器
2. main对f的调用以及f对g的调用都使用了inline function，体现在将立即数移入寄存器a2中
3. 0x73e
4. ra的值是0x3c，在调用jal指令时，ra寄存器被设置为下一条指令的指令，进入printf后ra寄存器会被保存到栈上
5. HE110 World，i应该修改为0x726c64，57616不用改
6. 编译器报错未定义行为或者输出垃圾值，因为参数不够，printf会默认从a2读取参数，a2寄存器上的值是未定义的